# Optional live market-data endpoint. Leave unset to use the built-in simulator.
# Point this at your data vendor's OHLCV REST endpoint and wire LiveHttpProvider
# in src/App.tsx (see src/engine/liveData.ts).
VITE_DATA_URL=
VITE_DATA_API_KEY=
